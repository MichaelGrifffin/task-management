# Server Port (Render sets this automatically)
PORT=5000

# Environment Mode
NODE_ENV=production

# JWT Authentication Secret Key (Set a strong random secret key)
JWT_SECRET=your_random_secure_jwt_secret_key
